<h1>ユーザー詳細</h1>

<p>名前: <%= @user.name %></p>
<p>メールアドレス: <%= @user.email %></p>

<%= link_to '一覧に戻る', users_path %>
<%= link_to '編集', edit_user_path(@user) %>
<%= button_to '削除', user_path(@user), method: :delete, data: { turbo_confirm: '本当に削除しますか？' }, class: 'btn btn-danger' %>
